function mergeSort(unsortedArr) {
    // we gonna need a mid, low, and high

    if (uns)
    let high = arr.length - 1,
        low = arr[0]
        mid = Math.floor(( low + high) / 2)

    let sortedArr = [];


}