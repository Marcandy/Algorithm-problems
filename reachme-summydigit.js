



// first map through the last element keep incrementing by initial value

function sumDigNthTerm(initval, patternl, nthterm) {
    // your code
    for(var i = 1, j; i <= initval; i++) {

      initval += patternl[j];
      if(j < patternl.length -1) {
        j++
      }else {
        j = 0
      }
    }

    initval.toSting().spl
}
